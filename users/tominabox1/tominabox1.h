#pragma once
#include "quantum.h"

// #include "wrappers.h"

// enum layers { _BASE, _NUM_SYM, _RAISE, _MACRO, _NUMPAD, _NAV, _FKEY, _GAME, _NP, _ADJUST };

// enum custom_keycodes { BASE = SAFE_RANGE, RGBRST, SPONGEBOB, AESTHETIC };

// enum { TD_DLR_LP, TD_AMP_RP, TD_QUESDOT, TD_RESET, TD_SLS_ENT, TD_DCAP };
